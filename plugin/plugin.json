{
  "name": "cursor-delegate",
  "version": "0.1.0",
  "description": "Delegate coding/research tasks to Cursor's models via the local cursor-agent CLI, with tiering, sandboxing, an async job model, and ground-truth git verification.",
  "mcpServers": ".mcp.json"
}
